"""Alpha motors Supply Chain team is on a happy hour today and planned to use python code to determine as who will pay the bill :)"""

import random
# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")

#Get the total number of names on the list today
num_items =  len(names)
#Random funtion with range
random_choice = random.randint(0,num_items-1)

Lucky_one_to_pay_the_bill = names[random_choice]
print (f"{Lucky_one_to_pay_the_bill} is going to buy the meal today")
